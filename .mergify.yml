queue_rules:
  - name: default
    merge_conditions:
      - check-success=gate
pull_request_rules:
  - name: queue-on-label
    conditions:
      - label=ready-to-queue
    actions:
      queue:
        name: default
