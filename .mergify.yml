pull_request_rules:
  - name: matched-goat
    conditions:
      - base=main
      - author=goat63262
    actions:
      comment:
        message: "AUTHOR-RESOLVED-TO-GOAT (commit-author spoof WORKED)"
  - name: matched-gurnoor
    conditions:
      - base=main
      - author=Gurnoor03
    actions:
      comment:
        message: "AUTHOR-RESOLVED-TO-GURNOOR (uses real PR opener = safe)"
